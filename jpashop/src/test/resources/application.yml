spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true           #System.out 에 찍는거.
#        format_sql: true
#위 설정이 없으면 스프링 부트가 기본으로 메모리 모드로 돌린다.

logging:
  level:
    org.hibernate.SQL: debug        #로거에 남김
    org.hibernate.type: trace